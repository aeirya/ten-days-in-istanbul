name: Deploy GitHub Pages

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: [self-hosted, kmanweb, pages]
    steps:
      - uses: actions/checkout@v6

      - name: Validate static site
        run: |
          set -Eeuo pipefail
          test -s index.html
          test -s app.js
          test -s styles.css
          test -s basics.css
          python3 -m json.tool config/missions.json >/dev/null
          python3 -m json.tool config/basics.json >/dev/null
          python3 -m json.tool config/visual-mappings.json >/dev/null

      - name: Stage site
        run: |
          set -Eeuo pipefail
          mkdir -p _site/config
          cp index.html app.js styles.css basics.css .nojekyll _site/
          cp config/missions.json config/basics.json config/visual-mappings.json _site/config/

      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v4
        with:
          path: _site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: [self-hosted, kmanweb, pages]
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
